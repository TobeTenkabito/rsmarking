[pytest]
testpaths = tests
python_files = test_*.py *_test.py
markers =
    benchmark: performance and latency checks; run explicitly for release/performance validation.
    integration: tests that exercise service or database boundaries.
    e2e: browser or full workflow tests.
addopts = -ra
